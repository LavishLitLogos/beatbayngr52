BeatBayngr™ fixed Netlify package

Deploy instructions:
1. Unzip this package.
2. Drag the CONTENTS of this folder into Netlify, or upload this ZIP directly.
3. index.html is at the root of the ZIP.

This fixed build uses relative paths plus inline CSS fallback so it will not render as raw unstyled HTML if opened locally or on Netlify.
